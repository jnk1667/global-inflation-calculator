import type React from "react"
export const metadata = {
  title: "FAQ Diagnostic Tool | Admin",
  description: "Diagnose and fix FAQ category issues",
}

export default function AdminFAQDiagnosticLayout({ children }: { children: React.ReactNode }) {
  return children
}
